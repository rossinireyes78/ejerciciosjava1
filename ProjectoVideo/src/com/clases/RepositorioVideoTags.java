package com.clases;



public class RepositorioVideoTags {
	//private ArrayList<String> tags = new ArrayList<>();
	//private List<Video> videos = new ArrayList<Video>();

	// agregar videos
	/*public List<Video> getAllVideos() {
		return new ArrayList<>(videos);
	}

	public void agregarVideo(Video nuevoVideo) throws Exception {
		if (nuevoVideo == null)
			throw new Exception();
		videos.add(nuevoVideo);
	}

	// agregar tags
	public List<String> getAllTags() {
		return new ArrayList<>();
	}

	public void agregartags(String etiquetas) throws Exception {

		if (etiquetas == null)
			throw new Exception();
		tags.add(etiquetas);
	}
	/*  public  void  setVideos ( List < Video >  videos ) {
	        this . videos = videos;
	    }
	  public  void  enterVideoToList ( Video  video ) {
	        videos.add(video) ;
	        setVideos (videos);
	    }*/

}
